% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/synthetic_population.R
\name{read_synthetic_population}
\alias{read_synthetic_population}
\title{Read a synthetic population from your own files or data frames}
\usage{
read_synthetic_population(people, schools = NULL, workplaces = NULL)
}
\arguments{
\item{people}{Path to a file, or a data frame, with one row per person.}

\item{schools}{Optional path to a file, or a data frame, with one row per
school.}

\item{workplaces}{Optional path to a file, or a data frame, with one row per
workplace.}
}
\value{
A \code{helios_synthetic_population}, as described in \code{\link[=rti_population]{rti_population()}}.
\code{metadata$source} is \code{"user-supplied"}, and \code{metadata$version},
\code{metadata$fips}, \code{metadata$catch_all_schools} and
\code{metadata$synthetic_school_size} are \code{NA}. \code{people} only has a \code{school_id} or \code{workplace_id}
column if the input has one. \code{schools} or \code{workplaces} is \code{NULL} when not
given.
}
\description{
Reduces a synthetic population to what helios needs: each person's household,
age, school and workplace. Use this for data that \code{\link[=rti_population]{rti_population()}} can't
download, such as a local copy of an RTI or FRED extract, or a population you
built yourself.
}
\details{
Columns are matched by name, and other columns are ignored. Both the 2010 RTI
spellings and the FRED export spellings are accepted:\tabular{lll}{
   Table \tab Column \tab Accepted names \cr
   \code{people} \tab household (required) \tab \code{sp_hh_id}, \code{household_id} \cr
   \code{people} \tab age in years (required) \tab \code{age} \cr
   \code{people} \tab school \tab \code{sp_school_id}, \code{school_id} \cr
   \code{people} \tab workplace \tab \code{sp_work_id}, \code{work_id}, \code{workplace_id} \cr
   \code{people} \tab person \tab \code{sp_id} \cr
   \code{schools} \tab school (required) \tab \code{sp_id}, \code{school_id} \cr
   \code{workplaces} \tab workplace (required) \tab \code{sp_id}, \code{workplace_id} \cr
}


Empty strings, \code{"X"} and \code{NA} mean none. Files can be comma or tab
separated.

Household, school and workplace ids are renumbered to \verb{1, 2, ...}. When
\code{schools} or \code{workplaces} is given, ids are numbered by row of that table, and
every id in \code{people} must appear in it. Otherwise they are numbered in order of
first appearance.
}
\examples{
people <- data.frame(
  sp_hh_id = c(10, 10, 11),
  age = c(40, 8, 67),
  sp_school_id = c(NA, 5, NA),
  sp_work_id = c(7, NA, NA)
)
schools <- data.frame(sp_id = 5)
workplaces <- data.frame(sp_id = 7)
read_synthetic_population(people, schools, workplaces)
}
\seealso{
\code{\link[=validate_synthetic_population]{validate_synthetic_population()}} for the checks made.

Other data: 
\code{\link{baseline_household_demographics_usa}},
\code{\link{rti_cache_dir}()},
\code{\link{rti_population}()},
\code{\link{schools_usa}},
\code{\link{validate_synthetic_population}()}
}
\concept{data}
