% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rti.R
\name{rti_population}
\alias{rti_population}
\title{Synthetic population for a US county or state from RTI International}
\usage{
rti_population(fips, version = "2010_ver1", refresh = FALSE)
}
\arguments{
\item{fips}{A FIPS code as a character string: 2 digits for a state (e.g.
\code{"06"} for California) or 5 digits for a county (e.g. \code{"06075"} for San
Francisco). Numbers are not accepted, because they lose leading zeros.}

\item{version}{The synthetic population release. Only \code{"2010_ver1"} is
available.}

\item{refresh}{If \code{TRUE}, download the data again even if it is cached.}
}
\value{
A \code{helios_synthetic_population}: a list with
\itemize{
\item \code{people}: a data frame with one row per person and integer columns
\code{household_id}, \code{age}, \code{school_id} and \code{workplace_id} (\code{NA} means none).
\item \code{schools}: a data frame with columns \code{school_id} and \code{reference_size},
the school's number of students across the whole US synthetic population.
\item \code{workplaces}: a data frame with columns \code{workplace_id} and
\code{reference_size}, the workplace's number of workers across the whole US
synthetic population.
\item \code{metadata}: a list with \code{source}, \code{version}, \code{fips}, \code{n_people} and
\code{n_households}.
}

A county extract lists only the county's residents, so a school or
workplace that also has members from elsewhere has fewer people in
\code{people} than its \code{reference_size}.
}
\description{
Downloads the RTI International U.S. Synthetic Population for a county or
state, reduces it to the people, schools and workplaces helios needs, and
caches the result. Later calls for the same \code{fips} and \code{version} read the
cache and need no network access.
}
\details{
The data come from
\url{https://github.com/RTIInternational/SyntheticPopulations} and are licensed
under CC BY 4.0. Downloads can be large: a county is typically tens of MB, and
a large state over 1 GB.

Group quarters (such as care homes, prisons and dormitories) are not
included.
}
\examples{
\dontrun{
san_francisco <- rti_population("06075")
}
}
\seealso{
\code{\link[=rti_cache_dir]{rti_cache_dir()}} for where the data are cached.

Other data: 
\code{\link{baseline_household_demographics_usa}},
\code{\link{rti_cache_dir}()},
\code{\link{schools_usa}}
}
\concept{data}
