% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/interventions.R
\name{calculate_efficacy_from_ach}
\alias{calculate_efficacy_from_ach}
\title{Compute per-location intervention efficacy from baseline ACH}
\usage{
calculate_efficacy_from_ach(
  ach_values,
  parameters_list,
  setting,
  coverage_vector = NULL
)
}
\arguments{
\item{ach_values}{Numeric vector of baseline ACH values per location in
the setting.}

\item{parameters_list}{A list of model parameters as generated by
\code{\link[=get_parameters]{get_parameters()}} (and populated by \code{\link[=set_intervention_ach]{set_intervention_ach()}}).}

\item{setting}{Character. One of \code{"workplace"}, \code{"school"}, \code{"leisure"},
or \code{"household"}.}

\item{coverage_vector}{Optional 0/1 vector, of the same length as
\code{ach_values}, marking which locations are covered by the intervention (as
generated by \code{\link[=generate_intervention_switches]{generate_intervention_switches()}}). \code{NULL} (the default)
treats every location as covered.}
}
\value{
Numeric vector of per-location efficacies, length equal to
\code{length(ach_values)}. Each value is in \verb{[0, 1]}; uncovered locations
return 0.
}
\description{
Applies the Wells-Riley framework to compute per-location efficacy for an
installed intervention. For each covered location \code{i}:

\deqn{efficacy_i = 1 - p_{post}(i) / p_{pre}(i)}

where \code{p_pre} and \code{p_post} are the Wells-Riley infection probabilities
before and after the intervention's delta-eACH is added to that location's
baseline ACH. Uncovered locations get \code{delta = 0}, which gives
\code{efficacy = 0} and so leave FOI unchanged downstream.

The intervention's delta per location is determined by the intervention
object's \code{delta_function} (and \code{delta_params}, \code{variation_function},
\code{variation_params}); see \code{\link[=make_intervention]{make_intervention()}}.

Called once per setting from \code{\link[=generate_population_data]{generate_population_data()}}.
}
\seealso{
Other intervention: 
\code{\link{ach_to_efficacy}()},
\code{\link{efficacy_to_delta}()},
\code{\link{generate_intervention_switches}()},
\code{\link{generate_joint_intervention_switches}()},
\code{\link{generate_setting_intervention_switches}()},
\code{\link{make_intervention}()},
\code{\link{set_intervention_ach}()},
\code{\link{uv_to_delta}()}
}
\concept{intervention}
