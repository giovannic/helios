% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/variables.R
\name{generate_population_data}
\alias{generate_population_data}
\title{Generate a population for the given parameters}
\usage{
generate_population_data(parameters_list, synthetic_population = NULL)
}
\arguments{
\item{parameters_list}{A list of model parameters as generated by \code{get_parameters}}

\item{synthetic_population}{Optional data frame with one row per person and
columns \code{household_id} and \code{age}, and for \code{"rti"} sampling \code{school_id} and
\code{workplace_id} (\code{NA} meaning none), such as returned by \code{\link[=rti_population]{rti_population()}}.
With the \code{non_residents} parameter it also needs \code{school_full_size} and
\code{workplace_full_size}.
It isn't a parameter because it's large, and parameter lists are saved and reused. To
simulate it, generate the population first and pass it to
\code{\link[=run_simulation]{run_simulation()}} as \code{population_data}.}
}
\description{
This will sample individuals' initial state and assign them to households and
other locations. It also draws the baseline ACH and riskiness of each
location. Interventions are not part of the population: see
\code{\link[=generate_intervention_data]{generate_intervention_data()}}.
}
\details{
Households and schools and workplaces depend on the
\code{school_workplace_sampling} parameter:
\itemize{
\item \code{"reference"}: households are sampled with replacement from
\code{synthetic_population} when given, and from the bundled San Francisco
households (\code{\link{baseline_household_demographics_usa}}) otherwise. School
sizes are sampled from \code{\link{schools_usa}} and workplace sizes from a power-law
distribution. Every child goes to school and every other adult to work.
\item \code{"rti"}: the population is the whole of \code{synthetic_population}, with each
person simulated once in their own household, school and workplace. Each
school gets \code{ceiling(students / school_student_staff_ratio)} staff, drawn
at random from adults who have a workplace and no school, and who leave
their workplace. This needs a \code{synthetic_population} with \code{school_id} and
\code{workplace_id} columns, and \code{human_population} must be its number of
people: see \code{\link[=set_synthetic_population_size]{set_synthetic_population_size()}}. The population can't be
scaled down, because settings would be left with too few members.
}

A county extract only lists its residents, so its schools and workplaces are
smaller than in reality. With the \code{non_residents} parameter, each school and
workplace also has non-resident members, who aren't simulated:
\itemize{
\item A workplace has \code{workplace_full_size} members, less its resident workers
(including any who become school staff).
\item A school has \code{school_full_size} students and
\code{ceiling(school_full_size / school_student_staff_ratio)} staff, less its
resident students and staff.
}

Settings with more residents than their full size, or no full size, have no
non-residents. The counts are in \code{non_residents} of the result: see
\code{\link[=create_SE_process]{create_SE_process()}} for how they transmit.
}
\seealso{
Other variables: 
\code{\link{create_variables}()},
\code{\link{generate_initial_disease_states}()},
\code{\link{generate_initial_households}()},
\code{\link{generate_initial_leisure}()},
\code{\link{generate_initial_schools}()},
\code{\link{generate_initial_workplaces}()}
}
\concept{variables}
